# ==================================================
# LOF/ETF 套利检测配置
# ==================================================

import os
# 优先读环境变量（GitHub Actions 用），本地测试用下面的硬编码值
SENDKEY = os.environ.get("SENDKEY", "")  # 本地填入，部署时通过 GitHub Secrets 注入

# 套利触发阈值（%）
# 默认 0.8%，扣除申购费+赎回费后仍有利润空间
PREMIUM_THRESHOLD = 99.0   # 溢价套利对散户不可行（T+2时差），实际关闭
DISCOUNT_THRESHOLD = -2.5  # 折价套利阈值：2.5%折价扣除成本后净利约1%，安全边际足够

# 流动性过滤：成交量太小的基金套利风险高，直接过滤
MIN_VOLUME = 100  # 单位：手

# 推送控制：同一只基金 N 分钟内不重复推送
COOLDOWN_MINUTES = 30

# 市场保护：大盘单日跌幅超过此值时，禁止发出折价套利信号
MARKET_DROP_BLOCK = -1.0  # -1.0 表示跌超 1% 时阻断

# 逐基金赎回费率手动覆盖（仅填写已确认与自动查询结果不同的特殊基金）
# 系统默认使用自动爬取的真实费率；此表只覆盖已核实的例外情况
# 大多数权益型指数 LOF 的 <7 天赎回费为 1.5%（证监会底线）
FUND_REDEMPTION_FEE: dict = {
    # 示例（已核实为 0% 短期赎回费的纯债 LOF，如有请在此添加）
    # "XXXXXX": 0.0,
}

# 日志文件路径
LOG_FILE = "arbitrage.log"
